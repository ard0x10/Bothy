// How long the trash keeps something before the sweep clears it, and the folder
// it keeps it in. Shared so the screen that shows the trash quotes the same
// number the sweep actually uses.
export const KEEP_DAYS = 30
export const TRASH = '.trash'
